name: Scala CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: write  # wichtig für sbt-dependency-submission

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'sbt'

      - name: Run tests
        run: sbt clean coverage test coverageReport

      - name: Upload dependency graph
        uses: scalacenter/sbt-dependency-submission@v3
        # optional:
        # with:
        #   working-directory: .  # falls dein sbt-Projekt nicht im Repo-Root liegt
